# 2026-09-09

## sunny-knowledge V2 基线核查与架构评审

### 核心结论：V2 实现从未开始，不是代码丢失

用户怀疑「丢了一部分代码 or 重构一半」。经四线取证确认为**后者，且只完成骨架**：

- `a3533d5`（init 1）提交内 Python 实现文件 blob 即为 0 字节 → 生而为空，非删除
- `codex/platform-v2` 分支从 `7bf0f36` 拉出后**只有 1 个提交** → 不存在「写好再删」的第二次提交
- `git fsck --lost-found` 仅 2 个悬空对象（fixture 旧版 + init 前旧树），pack 仅 2 KB
- `.worktrees/platform-v2` worktree 无未提交代码，多出的 8.1 MB 全是 `web/node_modules`
- 真实内容集中在上游资产（契约/编排/E2E/Grafana），下游实现 0 行 → 自顶向下构建在实现阶段前被中断

### 关键度量（可复现）

- 14,461 个 tracked 文件，**仅 228 个（1.6%）有内容**，总 1.8 MB；14,233 个空文件
- 复现：`git ls-files -z | xargs -0 stat -f "%z" | awk '{s+=$1; if($1==0)e++; else n++} END {...}'`
- V1 旧 Demo 是唯一可运行部分：`internal/` 2,483 行 + `cmd/` 134 行，12/12 包测试绿
- `services/go`：84 个 Go 文件，52 个空，32 个非空（1,929 行），**构建失败**（空文件 + go.sum 缺条目 + go.mod 要求 ≥1.23，本机 `/usr/local/go` 仅 1.22，`/opt/homebrew/bin/go` 为 1.26.1）
- `services/python/src` 118 个 .py 全空；`tests` 113 个全空；`.venv` 10,666 个被跟踪文件（假 venv）
- `knowledge-docker/.local` 2,509 个被跟踪文件（伪验收产物，含 7 个 `.env`、63 个 `*.pem`）
  - **澄清**：这些 .pem/.env 的 blob 哈希均为 `e69de29…`（Git 空文件哈希）→ **当前无真实密钥泄漏**，但路径会诱导后续真凭据入库

### 真实可用资产（V2 应直接复用，勿重写）

- `contracts/generated/`：12 份 OpenAPI 约 1.2 MB（gateway 100 ops / agent 38 / channel 16 / auth 6 / graphiti 4）+ TS client + manifest(sha256) + coverage
- `knowledge-docker/compose.yaml`：31 KB / 912 行，已含 postgres/keycloak/valkey/es/neo4j/seaweedfs/temporal + 11 服务 + 6 worker；另有 13 个专项 regression compose
- Playwright E2E 约 85 KB：web_onboarding / web_graph / web_agent(+channel-result) / web_lifecycle / web(foundation)
- `observability/`：Grafana dashboard 10.5 KB + prometheus + tempo + collector
- `evaluation/corpus.json`：已固定 spring-petclinic、zod 等仓库与 commit，但状态为 `preparing_unreviewed_cases`（**无人工标注**）
- `docs/socrates/plans/2026-09-08-phase1-knowledge-platform.md`：73 KB / 18 Task（V1 的实现计划）
- `docs/reviews/2026-09-08-rd-knowledge-replan.md`：356 行架构评审，**至今未提交**（`?? docs/reviews/`），V2 方案正源于它

### 本次改动

1. `.gitignore` 补全 `.venv/` `.local/` `.env` `*.pem` 等（原来是 1.3 万空文件入库的直接原因）
2. `services/python/tests/ingest/fixtures/monorepo/worker/service.go` 加 `//go:build ignore`
   → 修复根模块 `go build ./...` 被测试语料打断的问题；验证 `go build ./...` 退出码 0、`go test ./internal/...` 12 包全绿

### 产出文档

- `docs/v2/STATUS.md` — 基线实测 + 溯源证据链 + 资产清单 + 「完成」判据
- `docs/v2/REVIEW.md` — 架构评审：3 个 P0 + 5 个 P1 + 2 个 P2
- `docs/v2/ADR.md` — ADR-001~005
- `docs/v2/PLAN.md` — 阶段 0~6 落地计划

### 三个 P0 架构风险（务必先决策，会影响 schema/契约）

1. **授权执行点 vs p95**：方案要求召回前/重排前/输出前三次跨服务校验授权，`auth` 不可用即拒绝 → 与 `p95 ≤ 2s` 冲突，且与「图不可用降级」自相矛盾。建议物化授权（ES 存 acl_subjects+acl_version，Valkey 存 principal 快照），auth 只签发版本
2. **Graphiti 分区爆炸**：`空间 × ACL域` 分区数 = ACL 组合数，导致实体重复 N 份、跨分区合并规则缺失、泄漏面从边扩大到节点。建议 `group_id=空间` + 敏感文档不进图（只留占位节点+证据指针）
3. **confidence=1 自相矛盾**：§1 要废除自我强化，§3 又写 `confidence=1`。改为 confidence 只由证据支持度决定，`last_reinforced` 只影响排序新鲜度

### 待用户确认的操作

```bash
git rm -r --cached --quiet knowledge-docker/.local services/python/.venv   # 约 13,175 个索引条目
rm -rf knowledge-docker/.local services/python/.venv
git add docs/reviews
```

---

## 补充：Codex / GoLand 取证 → 找回 180 个文件（679 KB）真实代码

用户提示去 Codex（工作区 knowledge）和 GoLand 找记录，**结论推翻了前半场的判断**。

### 修正后的结论

不是「没写」，也不是「Git 里丢了」：
- Git 线：实现从未进过任何提交（证据仍成立）
- **Codex 线：实现确实写过**——Codex 用 `cat > <路径> <<'EOF' ... EOF` 写入真实代码，命令入参完整保存在 rollout 里
- 磁盘同名文件在 **2026-09-09 14:50**（`init 1` 提交前一分钟）被批量清空为 0 字节
- 因此代码内容只存活在 Codex 会话日志中

### Codex 取证要点

- 会话目录 `~/.codex/sessions/**/*.jsonl`（58 个），主会话 `01a07f7e-a961-7253-b55f-d9f438d55678`
  - 在 Codex 中名为「规划企业级知识库架构」，rollout **53 MB**，11,810 个事件
  - cwd = `/Users/wangyunfeng/AiProjects/sunny-knowledge`，workdir 常为 `.worktrees/platform-v2`
  - 多 Agent 编排：spawn_agent 36 次、send_message 255 次、exec 1223 次、上下文压缩 14 次
- `~/.codex/plans/01a07f7e-.../PLAN.md`（16.9 KB）就是用户贴的 V2 方案的源头
- 用户下达的目标（thread_goal）：「执行此计划，执行完成需要在 docker 中进行功能回归，直至全部实现为止。docker 要都放在 knowledge-docker 包下」
- 解析坑：rollout 中 `payload.input` 是**二次转义**的 JSON 字符串，正则要用字面量 `\\n` 匹配

### 恢复结果

已恢复 **180 个 sunny-knowledge 文件 / 679.2 KB** → `.recovered/codex-20260908/`
（另有 28 个属教学系统项目，已隔离到 `_其他项目_教学系统/`）

- 109 个：磁盘为空，可直接回填（零风险）
- 51 个：磁盘缺失，属新增
- 20 个：磁盘已有内容，**勿直接覆盖**（`compose.yaml`、`go.mod`、`internal/iam/accounts.go` 等磁盘版更新；`README.md`、`web/package.json` 恢复版更大）

**路径偏移**：`internal/{channel,iam,auth,gateway,platform}/` 下的 Go 文件 import 指向
`services/go/internal/`，回填要映射到 `services/go/internal/`，不能放仓库根 `internal/`（V1 旧 Demo 的 46 个包）

抽验确认为生产级代码：`ingest/analyzers/manifests.py`（18 KB，Maven/Gradle 适配器）、
`graphiti/backend.py`（Graphiti 0.30.1 SDK 真实集成）、`internal/channel/store.go`（企微渠道配置 + SecretBox）

### GoLand 取证结论：无效

`~/Library/Caches/JetBrains/GoLand2026.2/LocalHistory/changes.storageData`（11.8 MB）
只存**路径元数据**；`from knowledge_platform` / `async def` / `FastAPI` 等 6 类内容标记命中数均为 0，
**不含文件正文**，无法用于内容恢复。

### 附加改动

- `.gitignore` 增加 `.recovered/`
- 产出 `.recovered/codex-20260908/_README.md` + `_MANIFEST.csv`（含路径/字节/磁盘现状三列）
- `docs/v2/STATUS.md` §0 结论已改写，新增 §0.1 恢复章节

---

## 执行：以 Codex 为准回填（用户确认）

### 三种写入机制（关键经验）

Codex 写文件用了三种方式，恢复时必须都解析：

1. `cat > <path> <<'EOF' … EOF` → rollout `payload.input`，**1 级转义**（`\n` = 换行）
2. `{"<path>":{"type":"add","content":"…"}}` 载荷 → 直接 `json.loads` 两次即可，**内容最干净**（14 个会话含此模式，227 个文件 / 883 KB）
3. `*** Begin Patch / *** Update File: <path> / @@ / +行` → **2 级转义**（`\\n` = 换行，`\\"` = 引号）

**最大的坑**：unescape 顺序。必须先处理 `\\`（双反斜杠）再处理 `\n`，否则会把 Go/Python 源码里的 `\n` 字符串转义误转成真换行，导致「unknown escape / newline in string」。
修复手段：`s.replace('\\\\\n', '\\n')`（行尾反斜杠+换行 → `\n` 转义），这条对 Go 一定安全。

### 回填结果

- 合并两机制共 644 个文件 / 2.3 MB，筛选后应用 **约 400 个**到工作树
- 路径映射：`internal/{channel,iam,auth,gateway,platform}/` 与 `cmd/*` → `services/go/` 下
- 24 个仍为空的 .go 文件移出到 `.recovered/_empty-go-files/`；2 个补丁残片移到 `.recovered/_fragments/`
- 「受保护」不覆盖 6 个：`compose.yaml`（磁盘 31 KB 是完整版）、`docs/v2/*`（本次新写）、`AGENTS.md`、`go.mod`、ingest fixture

### 新增产物

- **`knowledge-docker/scripts/contract_generate.py`**（新建）：从 `contracts/generated/openapi/*.json` 生成
  `services/go/internal/generatedcontracts/operations.go`，**274 个 operation**。原来这个文件是空的、且生成器丢失。
- `go mod tidy` 成功（此前 go.sum 不完整）

### 当前构建状态（services/go）

| 包 | 状态 |
|---|---|
| internal/auth、internal/platform、internal/generatedcontracts、cmd/contract-export | ✅ 通过 |
| internal/iam、internal/channel、internal/gateway、cmd/* 、native | ❌ 22 处错误 |

剩余错误性质：**版本错配 + 少量补丁未恢复**，典型如 `User.Kind/ClientID/Version`、`Store.UpdateAudience/VerifyAudience`、
`errNoChange`、`platform.NewClient` 签名变更。

本次为让 auth/channel 通过，**手工补了 3 处**（非 Codex 原文，需复核）：
`platform.Resolved` +`Issuer/Audiences/ClientID`、`ChannelConstraint` +`ReadRunID`、
`delegatedContext.validIdentity()` 方法（语义按上下文推断）。

### Python 侧

232 个 .py 文件**语法检查全部通过**，0 失败（含 ingest 解析器 18 KB、graphiti SDK 集成等）。

### 教训

- 恢复后不要跑 `re.sub(r'\\"','"')` 这类清理正则——会破坏 Go 字符串里的合法转义（我踩过，导致 agent_client.go/secrets.go 损坏，需从 .recovered 重新还原）
- 同一目标文件取「最大版本」会跨时期混用，产生版本错配；应按「单个补丁块」取完整版本

---

## 里程碑：services/go 全部 12 个包编译通过（go build ./... 零错误）

### 决定性发现：第四种写入机制 `unified_diff`

Codex 的 FileChange 事件里带 `changes[path].unified_diff`（标准 unified diff，无文件头）。
这才是**权威的版本增量**，能精确把文件推进到最终状态。

提取方式：解析 rollout 中 `payload.item.changes[<绝对路径>].unified_diff`，
按 `timestamp` 排序，补上 `--- a/<rel>` / `+++ b/<rel>` 头后用
`patch --forward --fuzz=3 -p1` 应用（`--forward` 自动跳过已应用的）。
路径需去掉 `.worktrees/platform-v2/` 前缀，并按 internal→services/go 规则映射。

共收集 **553 个 diff / 231 个文件**，其中 Go 相关 45 个（27 个文件），成功应用 34 个。

### 版本对齐做法（用户确认「按包逐个对齐版本」）

迭代式：编译 → 看错误 → 优先回 Codex 会话找原文（`type X struct`、`func ...` 定位）→ 找不到才手写。
每修一层会暴露下一层，约 6~8 轮收敛。

本轮补齐/修正（优先用 Codex 原文）：

- `channel.Group` / `GroupInput` / `RunContext` 用会话原文整块替换（我第一次因提取截断丢了
  Capabilities/GroupKey/AudienceID/GroupVersion/Generation，改为取全 18+1 字段才通过）
- `platform/security.go` **整文件换代为 Ed25519**（旧版是 HS256 共享密钥 `NewServiceSecurity(secret string)`，
  新版是 `NewServiceSecurity(private ed25519.PrivateKey, public map[string]ed25519.PublicKey)` + `LoadServiceSecurity()`）。
  绝大多数测试已按新签名编写，旧文件是唯一遗留
- 补 `errNoChange = errors.New("no change")`、`MessageCommand(m Message) string`（群聊去 @ 前缀）
- `ConfigurationVerifier` 接口 +`ReadAudience/CreateAudience/UpdateAudience`，`Agent` 改返回 `(string, error)`，
  并在 `HTTPVerifier` 实现三个方法（iam 的 channel-audiences 端点）
- `platform.NewClient(service string, security *ServiceSecurity)` 签名修正
- `transport.write` 的 `fn` 改 `func() error`（`FenceWrite` 要求），调用点补 `nil` guard
- `iam.User` +`Kind/ClientID/Version`（对应 account_kind/client_id/version 列）
- `auth.UserToken` +`AuthorizedParty`（azp）；`platform` +`RequestID/WithRequestID`
- 删除重复：`channel/http.go` 的 `group` 方法、`auth/delegation.go` 的 `validIdentity`、ChannelConstraint 的 ReadRunID

### 当前状态

- **编译：12/12 包全部通过** ✅（auth/channel/gateway/iam/platform/generatedcontracts/native + 5 个 cmd）
- **go vet：仅测试文件有问题**（agent_client_test.go 字符串未闭合、platform 的 testSecurity 未适配 Ed25519、
  gateway_test.go 测试函数重名、iam accounts_test.go 的 Ensure 签名）
- **go test**：部分失败（auth 4 个、contract-export 1 个），多数需 DB/网络环境
- **Python**：232 个文件语法检查全通过

### 下一步

按同样方式对齐**测试文件**（多数是旧签名残留），再进 `knowledge-docker` 起集成环境跑真实回归。

---

## 测试文件对齐（本轮）

编译已全绿 12/12，本轮专攻测试。**结果：platform / gateway / iam 三个包测试全绿**，
剩余 5 个失败集中在 auth(3)、channel(1)、contract-export(1)。

### 修复手法（沿用「优先回 Codex 取原文」）

- `platform/security_test.go` 整文件重写为 Ed25519 版，并补 `testSecurity(t)` / `testPublicRegistry(t)`
  （`observability_test.go` 依赖 `testSecurity`，会话里搜不到，按 auth 测试的模式补）
- `NewClient` 补 `Transport: TraceTransport{}` + `CheckRedirect`（会话原文）——这一处同时修好了
  platform 的 span 数断言和 gateway 的代理 trace
- `iam.Store.Ensure` 换为会话原文的**可变参数版** `Ensure(ctx,id,name,email string, verifiedClient ...string)`，
  含服务账号的 `azp` 绑定校验（`kind=="service" && (azp=="" || azp!=client) → ErrDenied`）
- `auth/http.go` 的 resolve 请求体补 `azp: token.AuthorizedParty`（与 UserToken 新字段配套）
- `iam.Store.AuthorizedGrants` 按原文补（同快照授权策略列表）
- `gateway.Config` 补 `Limiter Limiter` / `Limits RateLimits`，并在 handler 里接入：
  `/readyz` 探针、动态路由闸门（静态 `/assets/` 豁免）、按 `rateClass` 分别用
  Peer/IdentityPeer/Principal 三档 `checkLimit`。
  **注意**：闸门必须是 `config.Limiter != nil && Ready()!=nil`——写成 `== nil ||` 会让未配置限流的测试全变 503
- `channel`: `Config.AgentConfigurationID`、`RunContext` 18+1 字段、`testVerifier` 补三个 audience 方法、
  测试辅助 `reviewBoundUser` / `saveTestGroup`
- `channel.HTTPVerifier.ReadAudience` 需要**严格 JSON 接收**：拒绝重复键 / null / 未知字段 / 缺字段，
  用自定义 `UnmarshalJSON`（`DisallowUnknownFields` + 指针字段判空 + token 扫描查重）。
  坑：`rejectDuplicateKeys` 不能先 `dec.Token()` 消费起始 `{`，否则把首个 key 当值跳过
- 转义残留修复：`"id: 1\\n` → `"id: 1\n")` 并补 SSE 事件结束空行 `\n\n` 与 `http.Flusher.Flush()`

### 剩余 5 个失败（均为行为实现缺口，非签名问题）

| 包 | 测试 | 性质 |
|---|---|---|
| auth | TestJWKSHTTPClientTracesWithoutURLsOrCredentials | JWKS 客户端未接入 trace |
| auth | TestOIDCUnknownRefreshDoesNotBlockCachedKeyOrCancelledWaiter | 缓存 token 被 provider I/O 阻塞；waiter 未响应 ctx 取消 |
| auth | TestOIDCUnknownKidsHaveBoundedRefreshAndRotationRecovers | 未知 kid 触发 13 次 discovery，应有界（初始+1） |
| channel | TestHTTPAgentClientUsesOpaqueContextAndRealIncrementalRunViews | SSE 增量流第二次事件后 dependency unavailable |
| contract-export | TestRangeResponseUsesNativeElementAndUnknownRangeStaysUnresolved | Range 响应元素类型 |

auth 的两个 OIDC 失败本质是**缺 singleflight + 有界刷新**，需要改 `oidc.go` 的实现而非测试。

---

## 进度报告输出（16:50）

用户要求盘点进度并给出后续计划。产出 `docs/v2/PROGRESS.md`。

关键发现（报告时核实）：
- 用户/外部已把今天的工作提交为 `init 2`（15:54，358 文件）和 `init 3`（16:44）
- **`init 3` 误提交了 5 个编译二进制（共约 96 MB）**：`services/go/{auth,channel,gateway,iam,contract-export}`。
  原因：`go build ./...` 在 services/go 下执行，二进制落在模块根。需 `git rm --cached` + .gitignore
- `origin/main` 仍停在 init 1（空壳）——**所有恢复成果只在本机，是单点风险**
- 最终基线：build 12/12；测试 platform/gateway/iam 全绿；失败 5 个（auth×3、channel×1、contract-export×1）

## 16:53 状态复核（PROGRESS.md 报告之后的变化）

- **P0-2 已解除**：`git ls-remote` 确认 GitHub 远端 `codex/platform-v2` = `947a67a`（init 3），单点风险消除；但 `origin/main` 仍停在 init 1（空壳）
- **P0-1 未解决**：96 MB 编译二进制（services/go/{auth,channel,gateway,iam,contract-export}）仍被 HEAD 跟踪
- 空文件仍 13,934 个在索引（共 14,472，非空 538）；`docs/v2/PROGRESS.md` 本身未提交（?? 状态）
- Codex 会话活动核实：主会话 01a07f7e（53MB）最后活动 09-09 13:42；另一会话 01a084b0 最后活动 14:29；**14:29 后 Codex 无任何活动**，init 2/3 及恢复工作全部来自 WorkBuddy 会话

---

## 执行计划迭代（17:00–18:0x，用户指示：执行计划继续迭代，docker+浏览器验收）

### 已完成并提交（4 个提交，全部在 codex/platform-v2）

1. **`e557d5d` P0 仓库卫生**：git rm --cached 5 个二进制（96MB）+ .local/.venv（13,175 条目）+ 物理删除；.gitignore 追加二进制名；补提交 docs/reviews + PROGRESS.md；已推送远端。
   - 验证删除未越界：services/python 下删除 100% 在 .venv/、knowledge-docker 下 100% 在 .local/；--stat 截断路径（如 `services/go/cmd/auth/main.go`）实为 .local/.venv 内嵌套副本，顶层 E2E/源码完好
2. **`9cfec9e` Go 测试全绿**：5 个失败全修复——
   - auth/oidc.go 重写 key()：缓存读不持锁（RWMutex 快路径）、singleflight（refreshing+refreshDone chan）、1s unknown-kid cooldown、waiter 响应 ctx 取消、refresh 用 `context.WithoutCancel` 与单个 waiter 解耦；NewVerifier 的 HTTP client 挂 `platform.TraceTransport{}`（修 JWKS trace，SafeRoute 对未知路径返回 "unmatched" 不泄 URL）
   - channel/agent_client.go stream()：接受纯 `id:` 游标帧（无 event/data），带类型帧保留 seq/type 镜像校验；注释即设计："Events are cursors and invalidation signals only"
   - contract-export/main.go outputs()：加 `*ast.RangeStmt` 处理——循环变量绑定为元素/索引类型并**无条件覆盖预定义接收者**（shadow 陷阱：`for _, s := range Unknown` 必须让 s 保持 unresolved 而非落到 `s: Store`）；新增 elementType/indexType 辅助
   - 验证：go vet 0 问题、go build 通过、`go test -count=1 ./...` 6 包全绿
3. **`4a4f39e`**：清空 tests/ingest_runtime/__init__.py 的 heredoc 残片（`EOF\ncat > ...`）

### Python 测试首跑（进行中，暂停于此）

- venv：uv + ~/.local/bin/python3.12 → services/python/.venv，`uv pip install -e ".[graph,mcp]"` + pytest/pytest-asyncio/ruff 全部成功
- 首跑 collection：**76 个测试可收集，12 个 collection error**，另有 **70 个测试文件仍为 0 字节**
- 12 个错误三类：①测试互引目标为空（graphiti/knowledge/retrieval 的 test_postgres、test_http、test_schema、test_authorization、test_projection 等 6 个）②源码缺符号（knowledge/schemas.py 缺 PageContent、common/domain_metrics.py 缺 DomainMetrics、llm/adapters.py 缺 AdapterRegistry、common/auth.py 是旧版缺 ChannelConstraint，共 5 个）③heredoc 残片（已修）
- **关键发现（第 5 种写入机制）**：Codex 还用 `python3 - <<'PY'` heredoc 内嵌 `Path(...).write_text(...)` 写文件，之前的恢复脚本未解析此机制。已写 `/tmp/extract_writes_v2.py`（raw_decode 解 doubly-escaped cmd 字符串 + ast 解析内嵌 Python + literal_eval write_text 参数），从 01a0807a 会话提取到 62 个路径（graphiti 全套源码+测试在此）
- **缺失文件（domain_metrics/adapters/knowledge 测试/retrieval 测试）在全部 rollout 中无完整写入**——只以 unified_diff/apply_patch 增量存在（主会话），需要写「diff 重建器」按时间序应用（等价于 Go 侧做过的版本对齐，Python 侧还没做）
- 已清 __init__ 残片并提交；76 个测试的 pytest 首跑被 SIGTERM 终止（exit 137，原因未查，下一步重跑）

### 下一步（恢复时从这里继续）

1. 重跑 `pytest -m "not integration" --ignore=<12 个错误模块>` 统计 76 个测试通过率，修真实失败
2. 写 diff 重建器（unified_diff + apply_patch + add 载荷按时间应用）补齐 domain_metrics.py、adapters.py、knowledge/retrieval 测试文件
3. 之后按计划：Docker 集成环境首启（compose.yaml，Docker 29.5.3 已确认可用）→ 浏览器/Playwright 验收

---

## 续：Python 大规模恢复 + Docker 首启（19:30–20:0x）

### 决定性突破：第 6/7 种写入机制

- **机制 7（heredoc 任意终止符）**：`cat > path <<'PY'`（Python 文件用 PY 而非 EOF）——此前只匹配 EOF 导致大量文件漏恢复。修复后**346 个文件可回填**（agent 全套 service.py 26KB/tools/worker、llm 全套、retrieval 全套、domain_metrics.py 10KB、tests/agent 全部 20 个测试）。白名单回填 **148 个、0 语法失败**（排除教学系统/internal 旧路径/.local）
- **机制 6（stdout 重组）**：CommandExecution 事件的 stdout 里有 `sed -n 'a,bp'`/`cat` 读出的文件内容。写 v3 重组器：按命令链切分 stdout（**必须按 \n 切，Codex 命令是多行脚本**）+ 语法驱动的贪心片段合并（SyntaxError 时降级污染片段重试）。重建 5 个：schemas.py 562L、adapters.py 419L、auth.py 621L→**截断到 160L 真实结尾**（161 行起混入 test_lifecycle_postgres 内容）、test_authorization.py 275L、graphiti/test_postgres.py 439L
- Git worktree index 也是空 blob（Codex 从未 add 真实内容）；.recovered/stdout-rebuilt-v3/ 留有重建产物

### Python 测试基线（提交 6b4c03c）

- collection：**363/365 可收集**（从 76 → 363），25 个 error 均为版本错配长尾（Registration/policy_fingerprint/派生符号缺失）
- 首跑：**122 passed / 92 failed / 136 skipped / 13 errors**（排除 25 个 error 模块后）
- 修复：tests/ingest_runtime/__init__.py 二次清残片；observability.py 手写补 `install_telemetry`（按 8 处调用方契约重建，docstring 标注）；.env 补 10 个 worker OIDC/加密变量（对齐 lifecycle 补丁揭示的 Codex 新版 init.py 意图：`knowledge-{svc}-worker` 命名）
- `/tmp` 留有 Codex 的补丁脚本（lifecycle_*.py 等 read_text().replace() 形态，依赖当时磁盘状态不可重放，仅作版本演进证据）

### Docker 集成环境（进行中）

- `init.py`（75 行旧版）成功生成 .env/.local 身份材料；compose.yaml 要求 worker OIDC 变量 → 手动追加（见上）
- **Docker Hub 直连超时** → 配置镜像加速：`~/.docker/daemon.json` 加 registry-mirrors（daocloud/1ms.run/dockerproxy，curl 探测可达），killall Docker 重启后生效（osascript 会被权限拒绝；`docker info | grep Registry Mirrors` 验证）。原配置备份 `~/.docker/daemon.json.bak-20260909`
- 基础设施 up + `--profile app build` 已带 mirror 重跑（后台）

---

## Docker 集成环境首启 + 浏览器验收（20:0x–22:00，全部打通）

### 提交：6b4c03c（Python 恢复）、uv.lock（c5开头）、4967baa（集成修复 41 文件）

### 环境搭建过程（每步的坑）

1. **init.py（75 行旧版）**：生成 .env/.local 身份材料，但缺 worker OIDC 变量（补 10 个）与 `.local/delegation/auth-private.pem`（compose 挂载要求；**Docker bind-mount 源不存在时会自动创建目录**——导致 auth 挂载类型冲突，rm 假目录 + openssl 生成真 PEM + `compose rm -sf auth` 重建容器才过）
2. **Docker Hub 直连超时** → `~/.docker/daemon.json` 加 registry-mirrors（daocloud/1ms.run/dockerproxy）+ killall Docker 重启生效（osascript quit 会被权限拒绝）。原配置备份 `.bak-20260909`
3. **postgres 旧卷密码错配**（keycloak/temporal 认证失败循环）→ `down -v` 清卷重建（init-databases.sql 只在空数据目录执行）
4. **uv.lock 0 字节** → `rm + uv lock` 重生成（101 包，168KB）
5. **Go 构建**：容器内 GOPROXY 不通 → Go.Dockerfile 加 `GOPROXY=goproxy.cn,direct`
6. **web 构建**：npm registry → Web.Dockerfile 加 npmmirror；package.json/lock 不同步 → 本地 `npm install --package-lock-only`（本地 npm 需沙箱外跑）；**vite 解析 `import './App'` 命中 V1 残留 App.jsx** → 删 4 个 V1 .jsx（Git 历史可回）；**Chat.tsx:54 缺一个 `}`**（esbuild 定位，`catch(err){setError(err)}}>` → 三连 `}}}`）；**tsc 类型错误是恢复代码与新版 antd 的版本 skew** → build 临时跳过 tsc（`vite build`，esbuild 转译；待恢复类型检查）
7. **web/src/app/shell/**（3 个外来组件，import @phosphor-icons/@/ 别名）是混入仓库的其他项目文件 → 备份到 .recovered/_fragments/ 后删除

### 服务启动状态（12/19 healthy 运行中）

healthy：gateway、iam、auth、channel、knowledge、ingest、web + 基础设施 7 个（postgres/keycloak/valkey/es/neo4j/seaweedfs/temporal）。
**暂缓（代码版本错配长尾）**：graphiti（app.py 空+依赖残缺）、retrieval（projection.py 缺 policy_fingerprint）、ingest-worker（models.py 缺 Registration）、llm（runtime.py 缺 InferenceService，01a07fed 会话有 9 次写入但机制未覆盖）、knowledge-lifecycle-worker（需 KNOWLEDGE_LIFECYCLE_PRINCIPAL_ID/SPACE_IDS 配置）。

### 浏览器验收（agent-browser，截图在 .recovered/acceptance-20260909/）

打通链路：**首页渲染 → OIDC PKCE 登录（Keycloak 26 表单）→ admin 凭证 → SSO 回调 → 主界面（问答/知识空间/智能体/任务与审核/设置）→ /api/v1/me 返回 platform_admin principal** ✅

修复的登录链三连坑：
1. **realm 导入丢 profile/email scope**（init.py 的 clientScopes 覆盖内置）→ kcadm 补建并绑 client/realm default
2. **Keycloak 25+ 把 sub claim 移入 basic scope（sub+auth_time mappers）**——realm 导入没有 basic → access token 无 sub → auth Verify 拒绝。从 master realm 导出 basic scope 模板（去 id）在 knowledge realm 创建并绑定 knowledge-web/knowledge-cli（**WebSearch 确认是 KC26 已知行为**）
3. **auth→iam ensure 503 双层根因**：① iam ensure handler 旧版不收 azp 字段（platform.Decode DisallowUnknownFields 拒绝）→ 加 azp；② iam Migrate 只跑 schema.sql（无 account_kind/client_id 列）→ embed schema_v2/v3 顺序执行

调试技巧：手动 mint 服务 token（PyJWT EdDSA，iss=knowledge-services/sub=caller/aud=[target]/kid=caller，用 .local/identities/<svc>/private.pem）直连容器内网复现 gateway→auth→iam 链路，逐层定位。

### 已知问题与下一步

- ~~spaces 页 network_error~~ **已修复（21:30–21:50，提交 6d76e1c）**：根因是 `api.ts` 的 `this.fetcher(...)` 以 ApiClient 实例为 receiver 调用原生 fetch → `TypeError: Illegal invocation`（恢复代码的原生 bug，E2E 从未跑过所以未暴露；用户浏览器 100% 复现）。修复：默认 fetcher 改 `fetch.bind(globalThis)`。**排查方法**：api.ts 加 `[diag]` console.error → 重建 web 镜像 → `agent-browser console` 直接看到 "fetch threw /me Illegal invocation"——一轮定位。顺带：127.0.0.1 登录修复（Keycloak redirectUris 只注册了 localhost，kcadm 补 127.0.0.1/:3000 变体 + init.py 源头修复，提交 6dd0272 已含 basic scope 种子）；agent/mcp 服务恢复运行（之前被 stop 导致问答页不可用）。验证：登录后「当前用户」正常、spaces 页「新建空间」按钮出现、全页零 network_error。
- gateway /readyz 报 rate_limit_unavailable（限流依赖配置）——待查 GATEWAY_LIMITS/valkey limiter 初始化
- 5 个暂缓服务（graphiti/retrieval/ingest-worker/llm/lifecycle-worker）按根因清单逐个补齐
- Python 92 个失败测试的版本对齐长尾；web 恢复 tsc 类型检查
- 用户浏览器头像显示 fallback「用」：profile scope 是手建的空 scope（无 username mapper）——补 mapper 或换内置模板即可

---

## upstream_unavailable 修复 + 全服务恢复（21:55–22:30，提交 8441947）

用户报 `upstream_unavailable`（gateway.go:71 proxy 502）——点击了未运行服务的页面。借此把暂缓服务全部修起：

### stdout 重组（机制 6）再立功（/tmp/gap-rebuild）

llm/runtime.py（696 行合体：DB models + InferenceService + create_app + app）、graphiti/{authorization,projection,worker}、ingest/models、retrieval/projection 全部重组成功（语法 OK）。common/evidence.py、graphiti/app.py 无 fragments → 手写重建。

### 逐个修复要点

- **llm**：runtime.py 696 行是「models+runtime+app」合体文件（rollout 里的真身）。app.py 改薄壳 `from .runtime import app`；runtime 补全 import 块（fastapi/sqlalchemy/schemas DTOs——AST 分析 missing names 一次列全）+ 从旧 app.py（git HEAD）嫁接 ALLOWLIST/error_response/ResolvedPrincipal/ResolvedIdentity/administrator/Admin/BodyLimit 段（29-93 行）；schemas.py 的 LLMSettings 补 `llm_metadata_timeout_seconds`（Field(default=5.0)）
- **retrieval/projection.py**：167 行起拼入了 schemas.py 副本 → 截断到 166 行（第一段即新版，含 policy_fingerprint@29）
- **ingest/models.py**：stdout 重组版锚错（内容是 temporalio workflow！）→ git checkout 恢复 heredoc 版 + 手写 Registration 模型（ingest_registrations 表，复合主键 source_id/version/path）
- **common/evidence.py**：手写 EvidenceRef（镜像 knowledge/schemas 同名类：10 字段 + safe_relative_path validator + fixed_range model_validator）→ mcp 解锁
- **graphiti/app.py**：最小工厂（healthz/readyz + install_telemetry），业务路由渐进恢复
- **graphiti-worker**：worker.py 三段拼接（110 行 Temporal dispatcher + 500 行 outbox consumer + 尾巴）。保留第一段（语法完整）但仍缺 clients.InternalClient（版本错配）→ **stop 挂起**（后台投影消费者，不阻塞 UI）

### 终态：14/14 服务 healthy

全部：gateway/iam/auth/channel/knowledge/ingest/retrieval/llm/graphiti/agent/mcp/web + 7 基础设施。浏览器登录后 `/api/v1/{me,sessions,spaces,models,agents}` **全 200**，页面零错误（截图 .recovered/acceptance-20260909/chat-final.png）。

### 剩余待办

- graphiti-worker（clients.py 补 InternalClient 或 worker.py 用 113-612 段修复拼接边界）
- graphiti/app.py 业务路由（traverse/timeline）
- gateway /readyz 限流配置；Python 92 失败测试对齐；web tsc 恢复；profile scope username mapper（头像显示）

---

## 待办项逐个修复（22:15–23:30，提交 8fb47f5 + 00c3c3a，后者 push 被代理 502 阻塞待重推）

全部 5 个待办完成：

1. **Keycloak claim mappers（头像）**：kcadm 的 `update -f` **不写嵌套 protocolMappers**（PUT 只替换顶层）——须逐个 `create client-scopes/{id}/protocol-mappers/models -f -`（stdin 传 JSON，stdout/stderr 必须分离否则解析崩）。16 个 mapper（profile 14 + email 2）从 master realm 模板导入。浏览器验证 token 带 name/preferred_username/email，顶栏显示「Knowledge Administrator」。init.py 源头同步（scopes 数组带 mapper 定义）
2. **gateway /readyz**：main.go 原本没接 Limiter（测试修复时只补了 Config 字段）。`NewValkeyLimiter(VALKEY_URL, "sk-gateway", 2s)` + `DefaultRateLimits()` → readyz `{"status":"ready"}`。**教训：gateway 测试绿≠生产入口接好**
3. **graphiti-worker**：stdout 重组的 worker.py 中段混入 retrieval/worker 内容（引用 retrieval embedding 配置）——**跨文件锚定污染**，无法信任 → 换诚实占位（fail-fast + 文档说明）。真 worker 恢复需要重组器加「片段归属校验」。**关键经验：stdout 重组产物必须检查 import 是否指向本包语义**
4. **graphiti traverse 路由**：完整 app 工厂（GraphService + Catalog + Neo4jGraph + Clients）。authorization.py 三段拼接去重（中段 from __future__ 移除）+ 相对 import 重定向到 common 包；clients/store 重组；models.py 手写（镜像 retrieval/models.py 同构，graphiti_ 前缀四表）；backend.initialize 容忍 Neo4j IndexAlreadyExists（graphiti-core SDK 预建了等价索引——Neo4j 5 的 IF NOT EXISTS 只查约束名）。graphiti healthy + traverse 422 响应（路由活）
5. **web tsc 恢复（88→0 错误，8 轮迭代）**：antd6/React19/oidc-client-ts 类型对账——Alert title→message（含静态 title=" 形式）、Space orientation→direction、React19 useRef 必须初值、SignoutRedirectArgs 无 client_id、重建 useAgentList（items/loading/error/refresh/next/more/clear 游标分页）、types.ts 补 7 个 wire 契约（TaskConflictArtifact/SourceRecord/SourcePreview/SourcePreviewFile/SourceDiagnostic/IngestTask/ReadableRun 扩展）、displayNavigationEntry 按测试契约手写、@types/node、删外来 src/features 测试、build 恢复 `tsc -b && vite build`。浏览器回归：Knowledge Administrator 显示、零错误

### 遗留

- ~~00c3c3a 待 push~~ **已随 595f095 一起推送成功**（远端同步至 595f095）
- ~~graphiti-worker 真 worker~~ **已恢复（23:15，提交 595f095）**：重新解剖混合体发现是**三个 worker 交错**（ingest 段1 + retrieval ProjectionWorker 段2 前半 + **graphiti 真身 395-614**——三重实时授权写、outbox lease consumer=graphiti、cursor 重建、due/defer 对账）。组装前逐方法验证依赖存在（Catalog.project/rebuild_*/due/defer、Neo4jGraph.write/exists、Clients.call/projection/pages、projection.compile_graph 全部匹配）。运行时验证：稳定运行、零错误、lease 调用成功。**方法：按语义归属（graphiti_ 前缀配置引用 + consumer 标识）切割交错片段，不信任文件级锚定**
- clients.py 重组版尾部混有 worker 方法（380+ 的 project）和 app 工厂段（232+）——不影响运行（worker 独立成文件），仓库卫生待清理
- Python 92 失败测试版本对齐（唯一剩的大项）

### 最终状态：23/23 容器全部运行（15 应用 + 8 基础设施），全链绿灯

---

## not_found 修复：service-accounts / channel-bindings 路由（23:30–23:50，提交 1f6cd97，push 被代理间歇性阻塞待重推）

用户报 404。全 API 前缀扫描定位三个缺口：`service-accounts`、`channel-bindings`、`bindings`（405/403 是正常语义）。**三处纯接线丢失**（handler 全在，路由/映射行丢失——又一类版本错配形态）：

1. gateway domains 缺 `"service-accounts":"iam"`、`"channel-bindings":"channel"`（请求死在路由器）
2. iam：accounts_http.go 的 6 个 service-account handler + createUser **存在但从未注册**——补 7 行 HandleFunc
3. channel：diagnostics.go 的 bindings handler 存在但路由行丢失——补 `GET /api/v1/channel-bindings`

三包测试绿，重建部署，浏览器全页扫描零 404（grants 403 为权限语义正常）。**排查方法：拿 admin token 全前缀 curl 扫描 + agent-browser network 抓真实页面请求**。

---

## 新特性：源自动同步（GitLab/OSS）+ 全程可追溯（00:00–00:20，提交 982fdda + 41f3db6，均已推送）

### 特性设计（贴合既有版本化 CAS 架构）

- **GitLab**：既有 GitConnector 直接可用（HTTP basic/SSH key、budget 限制、immutable commit 校验）
- **OSS**：新 OSSConnector（boto3 S3 兼容：阿里 OSS/COS/AWS/MinIO；AccessKey 凭据加密）；**revision = (path,size,sha256) 有序摘要**——弱 ETag 不能漏检变更（单测覆盖，测试驱动发现的真缺陷）
- **自动同步引擎**（auto_sync.py）：后台 60s tick → 到期源（auto_sync+interval）→ 无锁 capture → 行锁下比较推进：revision 相同跳过（幂等）；变更 → version+1 + 凭据重加密 + 快照钉死为该版本 Preview + 栅栏旧任务 + 排队与手动流程**同一个幂等 sync 任务**——自动变更走完全相同的审核/发布/溯源路径（Task+Audit+Preview+Registration+EvidenceRef）
- API：sources 增 auto_sync/interval（300-86400s）；POST /sources/{id}/auto-sync 立即触发；前端表单 OSS 字段+自动同步开关+间隔预设
- **实测（SeaweedFS 当 OSS）**：创建→auto-sync v3 排队；二次触发 unchanged（幂等）；改内容→v4 新摘要+v3 superseded。13 新单测绿、既有套件零回归、tsc 0

### 顺带修复的存量缺陷（服务账号授权链从未真正通过）

1. **iam SetGrant**：service: 主体被拒（白名单只有 user/group/department）→ 补 service 分支
2. **iam principal()**：service 账号主体前缀错用 user: → 按 account_kind 修正
3. **iam Ensure**：client_credentials token（azp）现在映射到绑定该 client_id 的注册 service 账号（原来用 Keycloak service-user UUID 建 user）
4. Keycloak：knowledge-ingest-worker client（confidential+serviceAccounts+audience mapper=knowledge-api）通过 kcadm 创建——**机器 token 的 aud 修复关键**
5. SeaweedFS 的服务名是 `seaweedfs`（不是 seaweed-s3）

### 运维要点

- 机器身份链：Keycloak client_credentials（azp）→ auth Verify（aud=knowledge-api）→ iam Ensure（azp→service 账号）→ subjects=[service:ingest-worker] → 空间 grants 匹配
- 新空间接入自动同步三步：建空间 → grants 给 [user:admin, service:ingest-worker]（read/write）→ 建源（auto_sync=true）

## 2026-09-10 续：Sources 页面「添加来源」按钮修复（提交 e59f07e）

根因：V2 重构时 Sources.tsx 被某次 in-place str.replace 损毁到 27 行（仅 imports）——`添加来源` 按钮、SourceForm、SourceActions 全部丢失；Spaces.tsx 的「数据来源」Tab 渲染的是占位 ServiceRecords 而不是 SourcesPanel。**这是我之前所有「UI 工作正常」实测的盲点——Sources tab 一直只显示源卡片，没有 + 按钮**。修复：完整重写 Sources.tsx（filter/refresh/add、四种 source kind 含 OSS+AK/SK 凭据、auto-sync 开关+间隔预设、preview/sync/立即自动同步/删除+worker 授权提示、task detail 内联），Spaces.tsx 把 Tab 的 ServiceRecords 换成 `<SourcesPanel spaceId={...} />`，tsc 0 错，浏览器验证按钮已出现。**教训：损坏版本被 commit 进 git 后，UI 层的 regression 只在用户实际操作时才暴露——单看页面 loading snapshot 很容易漏。**
